const BASE_URL = "http://127.0.0.1:4000/api/v1/user";

const endpoints = {
  signup: `${BASE_URL}/signup`,
  login: `${BASE_URL}/login`,
  profile: `${BASE_URL}/profile`,
};

export default endpoints;
